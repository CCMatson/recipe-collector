<%- include('./partials/html-head') %>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <%- include('./partials/nav') %>

    <body class="body-style">
      <main>
        <h1>Welcome to your Class Cookbook, <%= user ? user.profile.name : "friend" %>
        </h1>
        <% if (user?.profile.avatar) { %>
          <div>
            <img src="<%= user.profile.avatar %>" alt="<%= user.profile.name %> avatar" />
          </div>
          <% } %>
            <section class="section-middle">
              <h4>Collect your favorite recipe website urls and share them with friends</h4>
              <img class="image" src="/cook-book.png" alt="" />
            </section>
      </main>
    </body>
    <%- include('./partials/footer') %>